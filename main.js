const gameBoard = (function () {
  // code
  return {/* public methods */};
})();

const displayController = (function () {
  // code
  return {/* public methods */};
})();

const Player = function () {
  //code
  return {};
};